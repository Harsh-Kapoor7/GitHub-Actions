name: first-workflow

on:
  push:
    branches: ["main"]

jobs:
  job1: 
    runs-on: ubuntu-latest
    steps:
      - name: Step-1
        run: |
          pwd
          ls -ltr
          hostname
      - name: Checkout
        uses: actions/checkout@v6.0.2
      - name: Step-3
        run: |
          pwd
          ls -ltr
          hostname
